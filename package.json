{
  "name": "videographer-portfolio",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python3 build-portfolio-manifest.py && vite",
    "prebuild": "npm run check",
    "build": "node build-deploy.mjs",
    "check": "python3 build-portfolio-manifest.py --check",
    "verify": "node verify-dist.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
